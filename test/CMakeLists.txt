cmake_minimum_required(VERSION 3.24)

include(CTest)
if(BUILD_TESTING)
    find_package(Boost REQUIRED COMPONENTS unit_test_framework CONFIG)
    add_executable(test test.cpp)
    target_compile_features(test PUBLIC cxx_std_23)
    target_link_libraries(test PUBLIC yk_util Boost::unit_test_framework)
    add_test(NAME yk_util_test COMMAND test)
endif()
